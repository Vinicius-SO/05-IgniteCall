{
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts":{
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "eslint-plugin-prettier": "^5.0.1",
    "turbo": "^1.11.2"
  }
}
